[project]
name = "lol"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "coverage>=7.13.1",
    "pandas>=3.0.0",
    "prefect>=3.6.13",
    "pytest>=9.0.2",
    "requests>=2.32.5",
]
